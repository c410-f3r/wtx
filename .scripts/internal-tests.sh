#!/usr/bin/env bash

$(dirname "$0")/internal-tests0.sh \
    && $(dirname "$0")/internal-tests1.sh \
    && $(dirname "$0")/internal-tests2.sh \
    && $(dirname "$0")/internal-tests3.sh \
    && $(dirname "$0")/internal-tests4.sh